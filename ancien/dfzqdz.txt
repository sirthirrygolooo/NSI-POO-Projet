# Entrée utilisateur
entry = Entry(frame, font=("Arial", 20), fg='#0000FF')
entry.focus_set()
entry.pack(pady=25)

# Boutton pour valider l'entrée utilisateur
button = Button(frame, text="Valider", font=("Arial", 20), fg='#0000FF', command= lambda: display_text(entry)).pack(pady=20)

def display_text(entry):
    string = entry.get()
    print(string)